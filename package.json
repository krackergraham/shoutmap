{
    "name": "shoutmap-server",
    "version": "1.0.0",
    "dependencies": {
        "body-parser": "^1.9.0",
        "chalk": "^0.5.1",
        "cors": "^2.4.2",
        "express": "^4.9.8",
        "mongoose": "^3.8.18"
    },
    "engines": {
        "node": "0.10.x"
    },
    "scripts": {
        "start": "node app.js",
        "postinstall": "cd client && npm install && bower install && gulp"
    }
}
